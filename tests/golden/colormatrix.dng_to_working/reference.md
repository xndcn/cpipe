# colormatrix.dng_to_working golden reference

Source: deterministic cpipe self-reference generated by `tests/fixtures/gen_golden_isp_nodes.cpp`.

P3-PD-55 records why this fixture is still not RawTherapee-derived: Phase 3 T2 added
`tools/golden/rt_render.sh`, but the repo still lacks a node-matched color-matrix DNG/pp3 fixture
that RawTherapee can render to the same 4x4 stage output. The fixture uses the WB-emitted
`com.cpipe.wb.camera_to_xyz_d50_f32` metadata blob and the baked Bradford plus Rec.2020 constants.
