# tone.reinhard golden reference

Reference source: deterministic analytic Reinhard 2002 closed-form reference, generated by
`tests/fixtures/gen_golden_isp_nodes.cpp`.

The input is a synthetic scene-linear Rec.2020 RGBA gradient spanning 0..16. The reference applies
`x / (1 + x)` per channel and preserves alpha.
