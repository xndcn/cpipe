# P1 Golden Fixtures

These EXR pairs are deterministic P1 node goldens generated by:

```bash
cmake --build --preset linux-debug --target gen_golden_isp_nodes
build/linux-debug/tests/gen_golden_isp_nodes tests/golden
```

They are self-referenced fixtures for the current P1 node math, not
RawTherapee-derived references. See `docs/phase-01-walking-skeleton.md` PD-69.
