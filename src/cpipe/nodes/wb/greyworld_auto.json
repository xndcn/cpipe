{
  "$schema": "https://schemas.cpipe.dev/node/v0.1.json",
  "id": "com.cpipe.wb.greyworld_auto",
  "version": "1.0.0",
  "label": "Gray-World White Balance",
  "category": "color",
  "doc": "Estimates AsShotNeutral from demosaiced RGB means, applies white balance, and emits the interpolated camera-to-XYZ matrix.",
  "ports": [
    {
      "name": "rgb",
      "kind": "in",
      "caps": {
        "buffer_kind": "Image2D",
        "channels": ["rgba"],
        "precision": ["f16"]
      },
      "cardinality": "single",
      "metadata": {
        "requires_steps_applied": ["demosaic"],
        "requires_fields": [
          "calibration.color_matrix1",
          "calibration.color_matrix2",
          "calibration.calibration_illuminant1",
          "calibration.calibration_illuminant2"
        ]
      }
    },
    {
      "name": "rgb",
      "kind": "out",
      "caps": {
        "buffer_kind": "Image2D",
        "channels": ["rgba"],
        "precision": ["f16"]
      },
      "cardinality": "single",
      "metadata": {
        "inherits_from": "rgb",
        "sets_steps_applied": ["white_balance"],
        "writes_fields": ["capture.as_shot_neutral"]
      }
    }
  ],
  "params": [],
  "compute": {
    "device": "CPU",
    "engine": "Host",
    "in_pixel_bytes": 8,
    "out_pixel_bytes": 8,
    "scratch_pixel_bytes": 0
  },
  "color": {
    "input_role": "raw_camera",
    "output_role": "raw_camera",
    "respects_chromaticity": true
  }
}
